- hosts: web
  become: true
  tasks:
   - name: create new deployment
     command: kubectl apply -f playbooks/deployement.yml 
   - name: create new service
     command: kubectl apply -f  /var/lib/jenkins/workspace/aks-docker-jenkins-git-ansible/playbooks/service.yml
